Models folder

Define the data schema or a simple JSON store for articles.
Examples:
- `articles.json` — static demo data
- `article.js` — model wrapper for DB calls

If using a DB, put migration/seed scripts here.