Controllers folder

Add controller code that will fetch articles and pass them to views.
Example:
- `newsController.js` — fetch list, fetch details by id.

For a Node/Express app create functions that accept (req, res) and render the appropriate view.